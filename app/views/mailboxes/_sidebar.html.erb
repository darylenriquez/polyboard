<aside class="menu padded">
  <p class="menu-label">
    Mailboxes
  </p>
  <ul class="menu-list">
    <% mailboxes.each do |mailbox| %>
    <li><%= link_to mailbox[0], mailbox_path(mailbox[1]), class: is_active_class(:id, mailbox[1].to_s) %></li>
    <% end %>
  </ul>
</aside>